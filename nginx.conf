# ============================================================
# OpenINTJ Nginx 反向代理配置
# 将 HTTP 80 端口流量转发到应用的 8000 端口
# ============================================================

# ---------- HTTP 配置（纯 IP 访问模式） ----------
server {
    listen 80;
    server_name _;  # 匹配所有域名/IP

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # 请求体大小限制（支持图片上传）
    client_max_body_size 10m;

    # 访问日志与错误日志
    access_log /var/log/nginx/openintj_access.log;
    error_log  /var/log/nginx/openintj_error.log;

    # ---------- 反向代理到应用 ----------
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 超时配置（Agent 推理可能耗时较长）
        proxy_connect_timeout 30s;
        proxy_send_timeout    120s;
        proxy_read_timeout    120s;
    }

    # ---------- WebSocket 支持（预留） ----------
    location /ws {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_connect_timeout 7d;
        proxy_send_timeout    7d;
        proxy_read_timeout    7d;
    }

    # ---------- 静态资源缓存策略 ----------
    location /static/ {
        proxy_pass http://127.0.0.1:8000/static/;
        proxy_set_header Host $host;

        # CSS/JS/图片缓存 7 天
        location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            proxy_pass http://127.0.0.1:8000;
            expires 7d;
            add_header Cache-Control "public, immutable";
        }
    }

    # ---------- 健康检查端点（不记录日志） ----------
    location = /api/health {
        proxy_pass http://127.0.0.1:8000/api/health;
        access_log off;
    }

    # ---------- 禁止访问隐藏文件 ----------
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
}

# ============================================================
# HTTPS 配置模板（取消注释并填入证书路径即可启用）
# 前提：已申请 SSL 证书并上传到服务器
# ============================================================

# # HTTP → HTTPS 重定向
# server {
#     listen 80;
#     server_name your-domain.com;  # 替换为你的域名
#     return 301 https://$server_name$request_uri;
# }
#
# # HTTPS 主配置
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;  # 替换为你的域名
#
#     # SSL 证书路径（替换为实际路径）
#     ssl_certificate     /etc/nginx/ssl/your-domain.com.pem;
#     ssl_certificate_key /etc/nginx/ssl/your-domain.com.key;
#
#     # SSL 安全配置
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     # HSTS（启用后浏览器将强制使用 HTTPS）
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     # 安全头
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#
#     client_max_body_size 10m;
#
#     access_log /var/log/nginx/openintj_ssl_access.log;
#     error_log  /var/log/nginx/openintj_ssl_error.log;
#
#     # 反向代理（与 HTTP 配置相同）
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_connect_timeout 30s;
#         proxy_send_timeout    120s;
#         proxy_read_timeout    120s;
#     }
#
#     location /ws {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_connect_timeout 7d;
#         proxy_send_timeout    7d;
#         proxy_read_timeout    7d;
#     }
#
#     location /static/ {
#         proxy_pass http://127.0.0.1:8000/static/;
#         proxy_set_header Host $host;
#         location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
#             proxy_pass http://127.0.0.1:8000;
#             expires 7d;
#             add_header Cache-Control "public, immutable";
#         }
#     }
#
#     location = /api/health {
#         proxy_pass http://127.0.0.1:8000/api/health;
#         access_log off;
#     }
#
#     location ~ /\. {
#         deny all;
#         access_log off;
#         log_not_found off;
#     }
# }
